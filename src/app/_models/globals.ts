import { environment } from '../../environments/environment';

export const Globals = {
	serverAddress: "http://bleedingtiger.ddns.net:8088",
	minPasswordChars:8,
	maxPasswordChars:24,
	mobileWidth:640,
	log: true
}